import React from "react";

import { User } from "lucia";

const Main = ({ user }: { user: User }) => {
  return (
    <div className="min-h-screen">
      heell
    </div>
  );
};

export default Main;
